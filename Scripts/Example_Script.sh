#!/bin/bash                #shbang line Each Scripts should specify at the beginning, 
                           # in which shell it should be run. here is bash

echo "Running"             #will print Running on the command line
clear                      #clear the command screen
echo "text" >> file.txt    #append text to a file
date >> file.txt           #return the date and time
